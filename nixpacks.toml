providers = ["php"]

[phases.setup]
  nixPkgs = ["nodejs_22", "composer"]

[phases.install]
  cmds = [
    "npm install",
    "composer install --no-dev --optimize-autoloader --no-interaction"
  ]

[phases.build]
  cmds = [
    "npm run build"
  ]
